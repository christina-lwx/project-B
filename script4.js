function setup() {
  let canvas = createCanvas(800, 600);
  canvas.parent("canvasContainer");
  background(255);
}

function draw() {
  textAlign(CENTER,CENTER);
  textSize(50);
  text("happiness",width/2,height/2);

}